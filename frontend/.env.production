# Brukes automatisk av `vite build` (npm run build).
# Tom = relativ URL → request går til samme origin som frontend (https://verksted.poshub.no/api/...),
# nginx proxer videre til uvicorn på 127.0.0.1:8010.
VITE_API_BASE_URL=/api
